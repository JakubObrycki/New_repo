pipeline {
  agent any
  environment {
    //DOCKER_CREDENTIALS = credentials('jenkins-token')
    //dockerTag='zomaimage'
    //imageName='web'
  }

  stages {
    stage ('Integration with EC2'){
      steps {
          sshagent(['jenkins-aws']) {
            sh 'aws --version'
            sh 'ls -lt'
        }
      }
    }
  }
}
